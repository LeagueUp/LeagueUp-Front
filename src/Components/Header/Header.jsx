import React from 'react';
import SearchBar from './SearchBar/SearchBar';

const Header = () => {
  return (
    <div>
      <h1>search</h1>
      <SearchBar />
    </div>
  );
};

export default Header;
